// const sequelize = require('../config/connection');
// // const seedGallery = require('./userData');
// const seedSavedPets = require('./petData');

// const seedAll = async () => {
//     await sequelize.sync({ force: true });

//     // await seedUser();

//     await seedSavedPets();

//     process.exit(0);
// };

// seedAll();
